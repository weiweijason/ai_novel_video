# Video Worker 依賴套件
# 硬體需求: RTX 5080 16GB VRAM

# 基礎套件
requests>=2.31.0
minio>=7.2.0

# PyTorch Nightly (CUDA 12.4, 支援 RTX 50xx sm_120)
# 安裝方式: pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu124
torch>=2.7.0
torchvision>=0.22.0

# Wan 官方套件 (從 GitHub 安裝)
git+https://github.com/Wan-Video/Wan2.1.git

# Wan 依賴
transformers>=4.37.0
accelerate>=0.26.0
diffusers>=0.27.0
open-clip-torch
sentencepiece

# 影片處理
decord>=0.6.0
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
moviepy>=1.0.3
tqdm

# 基礎
numpy>=1.24.0
